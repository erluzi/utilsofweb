// want, return a fd function
// fd.add(config = {})
// fd()

// function fd(api, options) {
//   const _fd = fetch(api, options).then(res => {
//     setTimeout()
//   }).catch(e => {
//
//   }).finally()
// }
//
// fd.apis = {}
// fd.addApi = function (config: object) {
//   fd.apis = Object.assign({}, fd.apis, config)
// }
