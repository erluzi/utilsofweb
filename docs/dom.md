## Dom
- classie
  - hasClass(el, className)
  - addClass(el, className)
  - removeClass(el, className)
  - toggleClass(el, className)
  - has(el, className)
  - add(el, className)
  - remove(el, className)
  - toggle(el, className)

- $(selector, root)
- $$(selector, root)
